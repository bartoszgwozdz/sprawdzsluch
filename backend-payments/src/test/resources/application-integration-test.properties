# Konfiguracja dla testów integracyjnych backend-payments
# MongoDB URL nadpisywany przez Testcontainers (@DynamicPropertySource)
# services.pdf.url nadpisywany przez WireMock (@DynamicPropertySource)

paynow.apiKey=test-key
paynow.signatureKey=test-signature
paynow.apiUrl=http://localhost:9999/mock-paynow
paynow.redirectUrl=http://localhost/payment/return
paynow.notifyUrl=http://localhost/webhook

spring.data.mongodb.auto-index-creation=true
logging.level.dev.gwozdz=DEBUG
