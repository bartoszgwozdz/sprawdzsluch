apiVersion: v1
kind: Service
metadata:
  name: kafka-ui
  namespace: sprawdzsluch
spec:
  selector:
    app: kafka-ui
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    nodePort: 30080
  type: NodePort