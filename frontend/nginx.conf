server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Szczegółowe logi do debugowania
    error_log /var/log/nginx/error.log debug;
    access_log /var/log/nginx/access.log combined;

    # Dodaj headery z otrzymanym URI do debugowania  
    add_header X-Debug-URI "$uri" always;
    add_header X-Debug-Request-URI "$request_uri" always;
    add_header X-Debug-Original-URI "$http_x_original_uri" always;

    # Obsługa zasobów statycznych PRZED innymi lokalizacjami
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # Dokładne dopasowania dla podstron - PRZED ogólnym location /
    location = /test-sluchu {
        return 301 /test-sluchu/;
    }
    
    location = /test-sluchu/ {
        try_files /test-sluchu/index.html =404;
    }

    location = /faq {
        return 301 /faq/;
    }
    
    location = /faq/ {
        try_files /faq/index.html =404;
    }

    location = /kontakt {
        return 301 /kontakt/;
    }
    
    location = /kontakt/ {
        try_files /kontakt/index.html =404;
    }

    location = /o-nas {
        return 301 /o-nas/;
    }
    
    location = /o-nas/ {
        try_files /o-nas/index.html =404;
    }

    location = /results {
        return 301 /results/;
    }
    
    location = /results/ {
        try_files /results/index.html =404;
    }

    location = /polityka-prywatnosci {
        return 301 /polityka-prywatnosci/;
    }
    
    location = /polityka-prywatnosci/ {
        try_files /polityka-prywatnosci/index.html =404;
    }

    location = /regulamin {
        return 301 /regulamin/;
    }
    
    location = /regulamin/ {
        try_files /regulamin/index.html =404;
    }

    # Główna strona
    location = / {
        try_files /index.html =404;
    }

    # Fallback dla pozostałych ścieżek
    location / {
        try_files $uri $uri/ /index.html;
    }
}